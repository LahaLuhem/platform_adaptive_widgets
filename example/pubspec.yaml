name: example
description: "A new Flutter project."
publish_to: 'none'

version: 0.0.1

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  platform_adaptive_widgets:
    path: ../

  cupertino_ui: ^0.0.1
  gap: ^3.0.1
  go_router: ^17.3.0
  material_ui: ^0.0.1
  multi_value_listenable_builder_typed: ^0.2.1
  platform_icons: ^0.1.1
  pmvvm: ^4.0.1

flutter:
  uses-material-design: true
