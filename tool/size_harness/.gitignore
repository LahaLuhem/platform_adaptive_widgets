# Tracked: pubspec.yaml, lib/main.dart, .gitignore.
# Everything else is regenerated by `flutter create --platforms=android .`
# in the size-regression CI job — see .github/workflows/package.yml.

.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.idea/
.metadata
README.md
android/
ios/
linux/
macos/
web/
windows/
build/
pubspec.lock
test/
*.iml
